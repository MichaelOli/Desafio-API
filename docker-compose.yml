version: "3.8"
services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./desafio_api.db:/app/desafio_api.db
    networks:
      - mynetwork

networks:
  mynetwork:
